# simulator (aicapstone)

AICapstone-specific simulator config layer built on top of the upstream `leisaac` package.

Current scope:

- `src/simulator/tasks/cup_stacking/`
- `src/simulator/tasks/template/single_arm_franka_cfg.py`
